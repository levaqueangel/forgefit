export const dynamic = "force-dynamic";

export const metadata = {
  title: "Interface coach | APXFITNESS",
  robots: "noindex, nofollow",
};

export default function CoachLayout({ children }) {
  return children;
}
